# Path to the SQLite database file
db_path: __DATA_PATH__/cdir.db

# Path to the logging configuration file
log_config_path: __CONFIG_PATH__/log4rs.yaml

# ------------------------------------
# Path search settings
# ------------------------------------

# By default, text search includes the following fields:
# - The file path itself (e.g., /a/b/c/...)
# - For each shortcut the path is relative to, its name and description
#
# Set to false to search only file paths.
#
# path_search_include_shortcuts: false

# ------------------------------------

# Theme
# These ones are provided by default (check your "themes" directory):
# - default
# - dark-blue
# - dark
# - light-autumn
# - light-joy
# - pure
# - winter

#theme: dark-blue

# Custom date format (see: https://docs.rs/chrono/0.4.38/chrono/format/strftime/index.html)
# date_format: "%d-%b-%y %H:%M"

# Theme theme can be overriden here:
# inline_theme:
#   date: "#808080"
#   date_bold: true
#   path: "#000000"
#   highlight: "#eee680"
#   shortcut_name: "#008800"
#   description: "#808080"
#   ...
